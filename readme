html-editor

This is a very basic WYSIWYG HTML editor. It allows you to write
an HTML file from scratch. If you enable the 'every ... sec' option 
then the result window on the right will refresh every N seconds.

